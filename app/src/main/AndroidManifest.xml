<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="proyectoreq.sistemamuestreo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controladores.PantInicioSesion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controladores.MenuAdmin" />
        <activity android:name=".Controladores.pantOperacion" />
        <activity android:name=".Controladores.CrearActividad" />
        <activity android:name=".Controladores.AsignarAnalista" />
        <activity android:name=".Controladores.RegistrarUsuario" />
        <activity android:name=".Controladores.ConsultOperaciones"></activity>
    </application>

</manifest>